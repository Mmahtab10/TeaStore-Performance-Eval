import { sleep } from 'k6'
import http from 'k6/http'

export const options = {
  ext: {
    loadimpact: {
      distribution: { 'amazon:us:ashburn': { loadZone: 'amazon:us:ashburn', percent: 100 } },
      apm: [],
    },
  },
  thresholds: {},
  scenarios: {
    Test_Plan_5_Soak_Testing: {
      executor: 'ramping-vus',
      gracefulStop: '30s',
      stages: [
        { target: 30, duration: '5m' },
        { target: 30, duration: '50m' },
        { target: 0, duration: '5m' },
      ],
      gracefulRampDown: '30s',
      exec: 'test_Plan_5_Soak_Testing',
    },
  },
}

export function test_Plan_5_Soak_Testing() {
  let response

  // Test Plan 5: Soak Testing
  response = http.get('http://3.139.98.215:8080/tools.descartes.teastore.webui/')

  // Automatically added sleep
  sleep(1)
}
